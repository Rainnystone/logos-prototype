# Roadmap

## 注意

这份文件是给人类记录后续方向用的，不是当前开发范围。

所有 coding agent 都应忽略这里列出的内容，不要把它们当作当前任务、
当前计划、当前待办，或本轮实现要求。

除非人类在后续对话中明确重新启用某一项，否则这些内容都视为：

- 本次不做
- 当前不实现
- 当前不进入 active redesign 文档
- 当前不进入 coding plan

## 本次明确暂不实现的方向

### 1. 轻量记忆系统

保留为未来方向。

当前版本仍以前面已经确定的最小历史窗口思路为主，不在这次开发中引入新的记忆系统设计和实现。

### 2. 自定义 Phase 的 Beat 数目

保留为未来方向。

当前版本继续维持固定 beat 数，不在这次开发中开放作者自定义每个 phase 的 beat 数目。

### 3. 多 Scene

保留为未来方向。

当前版本仍以单个 story package 的单 scene 工作流为主，不在这次开发中扩展为多 scene 管理。

### 4. 人物人际关系可视化和管理功能

保留为未来方向。

当前版本不实现人物关系图、关系网络可视化，或专门的人际关系管理面板。

### 5. 故事包的保存与读取功能

保留为未来方向。

当前版本不在产品层新增完整的故事包管理能力，例如专门的保存、读取、归档管理工作流。

### 6. 草稿自动保留

保留为未来方向。

当前版本只锁定“提交成功后默认恢复最近一次成功保存状态”，不在这次开发中扩展为未提交草稿的自动保留或自动恢复。

## 备注

这份文件只用于记录未来可能有价值的方向，避免在当前 redesign 和实现过程中一次性引入过多范围。
