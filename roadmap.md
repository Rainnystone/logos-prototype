# Roadmap

## 注意

这份文件是给人类记录后续方向用的，不是当前开发范围。

所有 coding agent 都应忽略这里列出的内容，不要把它们当作当前任务、
当前计划、当前待办，或本轮实现要求。

除非人类在后续对话中明确重新启用某一项，否则这些内容都视为：

- 本次不做
- 当前不实现
- 当前不进入 active redesign 文档
- 当前不进入 coding plan

## 新模块方向

这些都只是未来方向，不是当前开发要求，也不应被当作本轮实现目标。

### 1. 基于 Beat Volume 和 skill 理念的轻量记忆系统

保留为未来方向。

### 2. 人物角色关系模块：可视化和可编辑

保留为未来方向。

### 3. 存档系统：故事包的存取

保留为未来方向。

### 4. 多 Scene 编排

保留为未来方向。

## 功能改进方向

这些也只是后续可考虑的改进项，不是当前开发要求，当前版本不需要为了它们调整实现范围。

### 1. 自定义 Phase 的 Beat 数

保留为未来方向。

### 2. 自定义段落梯度

保留为未来方向。

### 3. 人物角色系统进一步适配 RPG 的角色技能

保留为未来方向。

### 4. 选项生成系统兼容 RPG 的技能

保留为未来方向。

## 备注

这份文件只用于记录未来可能有价值的方向，避免在当前 redesign 和实现过程中一次性引入过多范围。
