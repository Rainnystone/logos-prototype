# Progress

## 2026-04-01

- 检查了 `planning-with-files-zh` 技能，确认它适合这次“事后补充可恢复上下文”的场景。
- 在项目根目录新增持久规划文件，用来记录 gossipelog agent 已完成状态和后续扩展方向。
- 已更新仓库首页说明，把 gossipelog agent 的完成状态、文档归档规则和后续页面拆分方向写清楚。
- 已完成文档一致性检查，准备把这组补充提交到主线。
- 开始一轮新的 roadmap 规划会话，接收并整理用户提出的 7 个未排序方向。
- 已把新的冻结约束记入规划文件：`coordinator` 不是 agent，`gossipelog agent` 才是当前第一个真正 agent。
- 已补看与路线讨论直接相关的现状代码，包括世界数据、关系数据和 workbench 运行态，准备进入 roadmap 拆分与优先级讨论。
- 已按用户要求开始纠正文档中的术语，把最核心的几份说明从“coordinator agent”改成“coordinator 是窄协调角色”，并明确 `gossipelog agent` 才是当前第一个真正 sidecar agent。
- 已启动 brainstorming visual companion，并生成第一张 roadmap 分组草图，准备用来收束第一阶段的路线重心。
- 用户补充说明浏览器只作为展示辅助，不需要把浏览器点击当成输入来源。
- 已根据当前仓库状态重组 roadmap，把原先的 A/B/C/D 讨论压成四阶段建议，并明确区分“关系状态查看”和“关系连续性”这两种不同性质的工作。
- 已继续补看运行态快照、authoring 持久化与 gossipelog 文件写入边界，用于回答 continuity、beat 回滚和新建故事包权限的问题。
- 已把这轮结论写入 `findings.md`，包括：continuity 不自动等于完整回滚、当前仍推荐 Phase 2 先于 Phase 3，以及导入 agent 需要一个受控的目标落点。
- 用户进一步把 rollback 定义成“从某个已接受 beat 的生成入口重新开跑”，而不是完整审计级重演。
- 已把这一定义和“受控新建能力”的实现方向写回 `findings.md`，用于下一步继续冻结 roadmap。
- 用户进一步补充：beat 级检查点也是未来“包内多条故事线”能力的基础，心智上应类比 Git branch。
- 已把这个补充写回 `findings.md`，并据此把 `Phase 2` 与 `Phase 3` 的关系明确成“先建检查点，再把检查点组织成故事线分支”。
- 用户进一步冻结了 storyline v1 的范围：第一版功能集合可以完整包含新建、切换、删除、重命名、归档、复制，但交付方式允许拆成更小节点逐步完成。
- 用户进一步给出编辑器壳子的重组意见：当前“控制台”页没有必要保留为正式页面，应改成“故事包管理”，而诊断与报错改为贴近当前页面的局部提示。
- 用户进一步冻结了“故事包管理”页的基本布局：左侧点选故事包，右侧显示并管理该故事包内部的故事线。
- 用户进一步冻结了右侧故事线工作区的展示方式：先看故事线列表，条目可依次展开，并通过下拉滑动区域管理，而不是 detail-first 结构。
- 用户进一步冻结了故事线条目动作的层级：继续与分叉不强行区分主次，更适合作为条目展开后的下拉动作组交给作者选择。
- 用户进一步冻结了故事线列表条目的显示密度：桌面端列表里直接展示当前 `phase / beat` 位置，不为手机端压缩信息。
- 已将当前 roadmap 正式写回 `task_plan.md`，重置为“当前真正活跃的计划”，不再沿用早期 gossipelog 文档补录目标。
- 已在 `findings.md` 中补全最终采用这条排序的原因，并把第一阶段的四个交付切片固定下来。
- 已把 `task_plan.md` 的当前阶段状态同步为：路线固化完成，下一步进入第一阶段正式 spec / plan。
