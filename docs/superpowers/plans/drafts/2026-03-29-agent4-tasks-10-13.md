# Agent 4 Output: Tasks 10-13 (Raw)

### Task 10: Simplify worldbase-cast.ts — delete parse/render functions, add CharacterProfile helpers

**Files:**
- Modify: `src/authoring/sections/worldbase-cast.ts`
- Modify: `src/authoring/sections/__tests__/worldbase-cast.test.ts`

DELETE: parseLegacyWorldBase, parseLegacyCharacterBlock, parseStructuredCharacterSection, parseStructuredFieldBlock, extractStructuredSection, stripFormatting, extractHeadingName, renderWorldBase, renderCharacterBlock, renderCharacterGroup, renderWorldHeaderBlocks, renderFieldLines, createWorldBaseCastDraft, normalizeCharacterDraft, WorldBaseCastDraft, WorldBaseCharacterDraft, createCharacterDraftId

KEEP: renderSupportingCast, CHARACTER_FIELD_DEFINITIONS, STRUCTURED_WORLD_HEADINGS, CharacterKind, normalizeBlock, normalizeInline

ADD:
- `createEmptyCharacterProfile(kind: CharacterKind): CharacterProfile`
- `normalizeCharacterProfile(character: CharacterProfile): CharacterProfile`

- [ ] **Step 1: Rewrite test file**
- [ ] **Step 2: Confirm RED**
- [ ] **Step 3: Rewrite worldbase-cast.ts**
- [ ] **Step 4: Confirm GREEN**
- [ ] **Step 5: Commit**

```bash
git commit -m "refactor: simplify worldbase-cast to direct CharacterProfile helpers"
```

---

### Task 11: Update WorldBaseCastSection.tsx and EditWorkbench.tsx for structured WorldBase

**Files:**
- Modify: `src/app/edit/sections/WorldBaseCastSection.tsx`
- Modify: `src/app/edit/EditWorkbench.tsx`
- Modify: `src/app/edit/__tests__/WorldBaseCastSection.test.tsx`

Key changes:
- `WorldBaseCastDraft` → `WorldBase`
- `WorldBaseCharacterDraft` → `CharacterProfile`
- `draftId` → `characterId`
- `supportingCast` → `npcCharacters`
- `locationPool` → `locationPatch`
- `createWorldBaseCastDraft(worldBase)` → direct use of `worldBase`
- `createEmptyWorldBaseCharacterDraft(kind, index)` → `createEmptyCharacterProfile(kind)`

- [ ] **Steps 1-7: Update fixtures, tests, components, commit**

```bash
git commit -m "refactor: wire editor surfaces to structured WorldBase"
```

---

### Task 12: Update persistence layer (bridge.ts, repository.ts)

**Files:**
- Modify: `src/authoring/persistence/bridge.ts`
- Modify: `src/authoring/persistence/__tests__/bridge.test.ts`

Key changes:
- `extractWorldBaseCastDraft` → `extractWorldBase` (validates CharacterProfile with `characterId`)
- Remove `renderWorldBase` call — pass structured WorldBase directly to `persistWorldBaseDraft`
- `isWorldBaseCharacterDraft` → `isCharacterProfile` (check `characterId` instead of `draftId`)
- `supportingCast`/`locationPool` → `npcCharacters`/`locationPatch`
- Remove `extractWorldBaseMainCharactersDraft` entirely
- repository.ts: no code changes needed (already spreads and writes YAML)

- [ ] **Steps 1-6: Update tests, bridge.ts, verify, commit**

```bash
git commit -m "refactor: persist structured WorldBase directly without render step"
```

---

### Task 13: Update remaining test files and run full test suite

**Files to update:**
- `src/engine/__tests__/fixtures/audit-loop-fixtures.ts` — worldBase → structured
- `src/engine/__tests__/schema-validator.test.ts` — verify PromptObject tests unchanged
- `src/engine/modules/__tests__/prompt-assembler.test.ts` — verify input is structured
- `src/app/components/FixtureReferencePanel.tsx` — display hero/coreCast/antagonists instead of mainCharacters
- `src/app/components/__tests__/FixtureReferencePanel.test.tsx`
- `src/app/api/authoring/packages/[packageName]/sections/[sectionId]/route.test.ts` — mock worldBase → structured
- `src/engine/__tests__/e2e/*` — verify with migrated sample-scene
- `src/engine/__mocks__/workbench-demo-adapter.ts` — verify no changes needed (uses PromptObject)

Key rule: `StoryPackage.worldBase` = structured WorldBase. `PromptObject.worldBase` = PromptWorldBase (3-string). Never confuse the two.

- [ ] **Steps 1-11: Update all remaining fixtures, run full suite, commit**

```bash
npm test
npm run type-check && npm run lint
git commit -m "test: migrate remaining fixtures and surfaces to structured WorldBase"
```
