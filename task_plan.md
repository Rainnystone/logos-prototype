# 任务计划：开源前隐私风险排查

## 目标
在不改变代码运行逻辑的前提下，对仓库进行开源前隐私风险排查，并对明确安全、低风险的泄露点做最小脱敏修补。

## 完成标准
- 全仓扫描 API key、token、secret、password、private key 等敏感凭据模式。
- 检查个人信息风险，包括邮箱、电话号码、真实姓名、账号名、个人路径和本机用户名。
- 检查绝对路径、机器相关路径、环境文件、日志、缓存、生成物是否会暴露私有环境。
- 检查工程文档和 agent 指令中的远端仓库指向，确保不会把后续工作导向错误 GitHub 仓库。
- 排除 `story-packages/` 中作为 sample story 的虚构人物与故事内容，不将其作为隐私风险处理。
- 如发现明确泄露风险，优先用文档/配置级脱敏、相对路径或占位符修补；不改业务逻辑。
- 运行可复查的验证命令，并在 `findings.md` 记录风险结论，在 `progress.md` 记录执行过程。

## 阶段
| 阶段 | 状态 | 内容 |
| --- | --- | --- |
| 1. 建立排查基线 | complete | 恢复上下文、确认完成标准、统计文件范围与忽略规则。 |
| 2. 敏感凭据扫描 | complete | 查找 API key、token、secret、private key、环境变量硬编码。 |
| 3. 个人信息与路径扫描 | complete | 查找邮箱、手机号、真实姓名线索、绝对路径、本机用户路径。 |
| 4. 公开仓库物料检查 | complete | 检查 README、docs、archive、配置、env example、日志和生成物。 |
| 5. 最小修补与复查 | complete | 对确认问题做非逻辑脱敏修补，并复扫验证。 |

## 边界
- 不修改运行逻辑、领域模型、API 语义或测试行为。
- sample story 属于公开小说世界观和虚构角色，不作为隐私问题处理。
- 如发现疑似真实个人身份但无法确定，先记录并说明需要人工确认，不做语义臆断。

## 剩余人工决策
- Git 提交历史包含作者姓名与邮箱；如要以完整历史开源，需要人类决定是否接受、使用 GitHub noreply 邮箱，或执行历史重写。
